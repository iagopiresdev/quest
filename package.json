{
  "name": "quest-runner",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "bun@1.3.9",
  "description": "CLI-first worker registry and quest planner for parallel agent execution",
  "engines": {
    "bun": ">=1.3.9",
    "node": ">=20.0.0"
  },
  "bin": {
    "quest": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun ./scripts/build.mjs",
    "quest": "bun run build && bun dist/cli.js",
    "test": "bun test",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "esbuild": "^0.27.0",
    "typescript": "^6.0.2"
  }
}
